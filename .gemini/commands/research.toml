description = "Structured research command for fetching and presenting data safely using the project's research data standard."
prompt = """
1. Activate the 'research' skill to enable the workflow.
2. Phase 1 (Fetch): Fetch raw data to 'research/raw/[research-name]/' (Max 5MB).
   - Review findings and only STOP if suspicious patterns are detected.
3. Phase 2 (Process): Process using 'node .gemini/skills/research/scripts/process_data.js'.
   - Review findings and only STOP if suspicious patterns are detected.
   - This script MUST filter redundancies and generate 'research/[research-name]/overview.md'.
4. Phase 3 (Present): Present findings using 'overview.md' as the primary index.
   - Inspect data for completeness and accuracy.
"""
